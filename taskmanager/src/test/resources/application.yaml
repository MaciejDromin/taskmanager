spring:
  transaction:
    default-timeout: 10s
  jpa:
    generate-ddl: false
    hibernate:
      ddl-auto: none
  liquibase:
    change-log: classpath:db/changelog/db.changelog-master.yml

scoring:
  limit: 4
